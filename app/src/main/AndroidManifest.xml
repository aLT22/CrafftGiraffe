<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="mn.factory.crafftgiraffe">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseTheme">
        <activity
            android:name=".ui.start.MainActivity"
            android:theme="@style/FullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.categories.CategoriesActivity"
            android:theme="@style/FullScreenTheme">
        </activity>
        <activity
            android:name=".ui.results.ResultsActivity"
            android:theme="@style/FullScreenTheme">
        </activity>
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:theme="@style/FullScreenTheme">
        </activity>
        <activity
            android:name=".ui.photos.AllPhotosActivity"
            android:theme="@style/FullScreenTheme">
        </activity>
        <activity
            android:name=".ui.orighami.OrighamiActivity"
            android:theme="@style/FullScreenTheme">
        </activity>
    </application>

</manifest>